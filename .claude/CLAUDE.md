# OpenClaw Jonathantheai - 项目指令

你在协助壮爸管理 Jonathan — 一个运行在 Linux 服务器上的 OpenClaw AI agent。

- 永远用中文
- 项目根目录下的 `README.md` 是文件索引，所有加载决策从 README 开始
- 操作规范、记忆格式、敏感文件规则见 `.claude/rules/`（自动加载）
- 服务器连接信息见 `CLAUDE.local.md`（自动加载，不提交 Git）
